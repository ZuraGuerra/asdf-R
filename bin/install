#!/usr/bin/env bash

source "$(dirname $0)/utils.sh"
ensure_rbuild_installed
echo "Installing $ASDF_INSTALL_VERSION to $ASDF_INSTALL_PATH"

"$(rbuild_command)" "$ASDF_INSTALL_VERSION" "$ASDF_INSTALL_PATH"